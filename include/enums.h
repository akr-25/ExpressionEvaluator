#pragma once

enum class ArithmeticOperations {
  ADD,
  SUBTRACT,
  MULTIPLY,
  DIVIDE  
};

enum class ComparisonOperations {
  EQUAL,
  NOT_EQUAL,
  GREATER_THAN,
  LESS_THAN,
  GREATER_EQUAL,
  LESS_EQUAL
};

enum class LogicalOperations {
  AND,
  OR,
  NOT,
  NONE
};

enum class DataTypes {
  BOOLEAN,
  INTEGER,
  DOUBLE,
  STRING
};